package de.uniba.wiai.dsg.ajp.assignment3;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.ValueSource;

import static org.junit.jupiter.api.Assertions.assertEquals;

class MovieTest {
/*
    @BeforeEach
    @Test
    public void childrensPriceCode() throws IllegalArgumentException  {
        Movie movie = new Movie("MovieX", 2);
        assertEquals(2, movie.getPriceCode());
    }
  @BeforeEach
    @Test
    public void regularPriceCode() throws IllegalArgumentException  {
        Movie movie = new Movie("MovieX", 0);
        assertEquals(0, movie.getPriceCode());
    }
  @BeforeEach
    @Test
    public void newReleasePriceCode() throws IllegalArgumentException  {
        Movie movie = new Movie("MovieX", 1);
        assertEquals(1, movie.getPriceCode());
    }
    @BeforeEach
    @ParameterizedTest
    @ValueSource(ints = {2, 0, 1})
    public void checkGetTitle(int value){
        Movie movie = new Movie("Movie",value);
        assertEquals("Movie", movie.getTitle());
    }
    @BeforeEach
    @Test
    public void chekSetTitle(){
        Movie movie = new Movie("", 0);
        movie.setTitle("Movie");
        assertEquals("Movie", movie.getTitle());
    }

    @BeforeEach
    @Test
    public void getChargeChildrensPrice() {
        Movie movie = new Movie("Movie", 2);
              double value = movie.getCharge(4);
              assertEquals(1.5, value);
    }


 */
}